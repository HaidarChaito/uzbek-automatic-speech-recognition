# Uzbek Automatic Speech Recognition (ASR)

Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install torch torchcodec torchaudio --index-url https://download.pytorch.org/whl/cpu
pip install jupyter notebook numpy pandas scikit-learn matplotlib soundfile tqdm jiwer scipy google-cloud-speech torchcodec protobuf datasets
```
