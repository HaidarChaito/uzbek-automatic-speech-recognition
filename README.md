# Uzbek Automatic Speech Recognition (ASR)

Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install jupyter notebook numpy pandas scikit-learn matplotlib soundfile tqdm jiwer torchcodec protobuf datasets
```
